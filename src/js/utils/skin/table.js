/**
 * CAOS STUDIO - Skin Coordinates Table
 * Define los sectores de la textura (Capa interna y externa)
 */

export const head = {
    inner: [8, 8, 8, 8],
    outer: [40, 8, 8, 8]
};

export const body = {
    inner: [20, 20, 8, 12],
    outer: [20, 36, 8, 12]
};

// Puedes añadir más partes aquí si luego quieres dibujar brazos o piernas